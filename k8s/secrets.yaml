apiVersion: v1
kind: Secret
metadata:
  name: account-secrets
data:
  # base64-encoded dev placeholder. Substituir antes do `kubectl apply`
  # em cluster real (idealmente via AWS Secrets Manager + External Secrets
  # Operator, ou patch via .env durante deploy).
  DATABASE_USERNAME: c3RvcmU=
  DATABASE_PASSWORD: c3RvcmU=
